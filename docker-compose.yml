services:
  crewmind:
    build: .
    image: crewmind/crewmind:latest
    container_name: crewmind
    ports:
      - "${APP_PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
